\documentclass[12pt]{article}
\usepackage{response}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{lipsum} % to generate some filler text
\usepackage{fullpage}
\usepackage{subfigure}
\usepackage{mathrsfs}
\usepackage{xr}
\usepackage{amsthm,cite,url,graphicx,booktabs,lipsum,color,bm,caption,subcaption,soul}
\usepackage{pifont,tikz,paralist,multirow,amssymb}
\usepackage{epstopdf}
\usepackage{xifthen}
\usepackage{enumerate}
\usepackage{titlesec,upgreek}
\usepackage{lineno}
\usepackage[table]{xcolor} 
\usepackage{amsmath}
\usepackage{listings}

\definecolor{myBlue}{RGB}{0, 0, 255}
\definecolor{myRed}{RGB}{255, 0, 0}
\definecolor{myPurple}{RGB}{199, 21, 133}
\definecolor{myGreen}{RGB}{0, 128, 0}

\title{Comments}

\begin{document}
	\maketitle

\begin{reviewer}
\noindent \textbf{Category: [Abbreviations] [5.2] [low]}

2, fourth paragraph, defining pdg
\begin{lstlisting}[breaklines=true]
	Particle Data Group~(PDG)~\cite{ref::pdg2024}
\end{lstlisting}

\textbf{Reviewer Comment:}

Unnecessary capitalization when defining the abbreviation PDG.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Particle Data Group (PDG)~\cite{ref::pdg2024}
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Abbreviations] [5.2] [low]}

3, paragraph 5, first sentence
\begin{lstlisting}[breaklines=true]
	Particle identification~(PID) for charged tracks combines measurements of the d$E$/d$x$ in the MDC and the flight time in the TOF to form likelihoods $\mathcal{L}(h)$ for each hadron hypothesis $h~(h=p, K, \pi)$.
\end{lstlisting}

\textbf{Reviewer Comment:}

The abbreviation 'PID' is defined with unnecessary capitalization ('Particle identification'). Use 'particle identification (PID)'.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Particle identification (PID) for charged tracks combines measurements of the d$E$/d$x$ in the MDC and the flight time in the TOF to form likelihoods $\mathcal{L}(h)$ for each hadron hypothesis $h~(h=p, K, \pi)$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

2, fourth paragraph, describing estimation software
\begin{lstlisting}[breaklines=true]
	or otherwise estimated with {\sc lundcharm}~\cite{Lundcharm00}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid using small caps for software names. Use standard font.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	or otherwise estimated with \textsc{Lundcharm}~\cite{Lundcharm00}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

2, fourth paragraph, describing modeling software
\begin{lstlisting}[breaklines=true]
	All particle decays are modeled with {\sc evtgen}~\cite{Lange01,Lange02}
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid using small caps for software names. Use standard font.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	All particle decays are modeled with \textsc{EvtGen}~\cite{Lange01,Lange02}
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

2, fourth paragraph, describing radiation package
\begin{lstlisting}[breaklines=true]
	is incorporated using the {\sc photos} package~\cite{PHOTOS}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid using small caps for software names. Use standard font.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	is incorporated using the \textsc{PHOTOS} package~\cite{PHOTOS}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

2, third paragraph, describing mc package
\begin{lstlisting}[breaklines=true]
	Simulated data samples produced with a {\sc geant4}-based~\cite{Geant4} Monte Carlo (MC) package
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid using small caps for software names. Use standard font.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Simulated data samples produced with a \textsc{Geant4}-based~\cite{Geant4} Monte Carlo (MC) package
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

2, third paragraph, describing generator
\begin{lstlisting}[breaklines=true]
	with the generator {\sc kkmc}~\cite{Jadach01}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid using small caps for software names. Use standard font.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	with the generator \textsc{KKMC}~\cite{Jadach01}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

3, paragraph 6, sentence about $k_s^0$ candidate reconstruction
\begin{lstlisting}[breaklines=true]
	The $ K^0_S$ candidate is reconstructed from two oppositely charged tracks satisfying $|V_{z}|<$ 20 cm, with no constraint applied in the $xy$ plane or PID requirement on charged pions.
\end{lstlisting}

\textbf{Reviewer Comment:}

The phrase 'no constraint applied in the $xy$ plane' is ambiguous. Specify what is not constrained.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The $ K^0_S$ candidate is reconstructed from two oppositely charged tracks satisfying $|V_{z}|<$ 20 cm, with no constraint on the distance of closest approach in the $xy$ plane and no PID requirement on the charged pions.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

5, paragraph 2, first sentence
\begin{lstlisting}[breaklines=true]
	The total number of $\psi(3686)$ events in data is measured to be $N_{\psi(3686)}=(2712.4\pm14.3)\times10^6$
\end{lstlisting}

\textbf{Reviewer Comment:}

The number 2712.4 has more significant digits than its uncertainty 14.3. The precision should match.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The total number of $\psi(3686)$ events in data is measured to be $N_{\psi(3686)}=(2712.4\pm14.3)\times10^6$
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

5, paragraph 5, first sentence
\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of $\Lambda$ reconstruction, including the tracking of the $p\pi^-$ pair, decay length cut, mass window cut, vertex fit and secondary vertex fit, are studied...
\end{lstlisting}

\textbf{Reviewer Comment:}

Subject-verb agreement error. 'Uncertainty' (singular) should take 'is studied', not 'are studied'.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of $\Lambda$ reconstruction, including the tracking of the $p\pi^-$ pair, decay length cut, mass window cut, vertex fit and secondary vertex fit, is studied...
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.3] [\textcolor{blue}{medium}]}

6, first sentence
\begin{lstlisting}[breaklines=true]
	Using $(2712.4\pm14.3)\times10^6$ $\psi(3686)$ events collected...
\end{lstlisting}

\textbf{Reviewer Comment:}

Sentence starts with a number in mathematical notation. Avoid starting sentences with symbols or numbers.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Based on $(2712.4\pm14.3)\times10^6$ $\psi(3686)$ events collected with the BESIII detector operating at the BEPCII collider,
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

1, first paragraph, second sentence
\begin{lstlisting}[breaklines=true]
	Experimentally and theoretically, their decays are not as extensively studied as those of the vector charmonium states $J/\psi$ and $\psi(3686)$.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid jargon 'extensively studied'. Use more precise scientific language.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Experimentally and theoretically, their decays have been studied less thoroughly than those of the vector charmonium states $J/\psi$ and $\psi(3686)$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

1, second paragraph, first sentence
\begin{lstlisting}[breaklines=true]
	To date, there have been many studies of $\chi_{cJ}$ decays involving $p\bar p$ pairs and meson pairs~\cite{ref::pppipi,ref::pppi0pi0,ref::ppkk}, and hyperon pairs~\cite{ref::lambdalambdabar,ref::sigmasigmabar} which can be described by the color octet mechanism~\cite{ref::oct1,ref::oct2}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid vague term 'many'. Use more quantitative language when possible.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	To date, numerous studies of $\chi_{cJ}$ decays involving $p\bar p$ pairs and meson pairs~\cite{ref::pppipi,ref::pppi0pi0,ref::ppkk}, and hyperon pairs~\cite{ref::lambdalambdabar,ref::sigmasigmabar} have been performed, which can be described by the color octet mechanism~\cite{ref::oct1,ref::oct2}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

1, second paragraph, third sentence
\begin{lstlisting}[breaklines=true]
	Measurements of this kind of decay are therefore important for a deeper understanding of $\chi_{cJ}$ decay mechanisms and for identifying potential intermediate-state decays.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid vague phrase 'this kind of'. Be more specific about the decay type.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Measurements of these decays are therefore important for a deeper understanding of $\chi_{cJ}$ decay mechanisms and for identifying potential intermediate-state decays.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

1, third paragraph, first sentence
\begin{lstlisting}[breaklines=true]
	Naive isospin symmetry indicates that the branching fractions of $\chi_{cJ}\to \Lambda K^0_S\bar \Xi^0$ are at $10^{-5}$-$10^{-4}$, which are detectable at BESIII.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid subjective term 'naive'. Use more neutral scientific language.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Isospin symmetry suggests that the branching fractions of $\chi_{cJ}\to \Lambda K^0_S\bar \Xi^0$ are at $10^{-5}$-$10^{-4}$, which are detectable at BESIII.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

3, paragraph describing $\bar \xi^+$ selection
\begin{lstlisting}[breaklines=true]
	PID is not required for $\pi^-$ meson; but is required for $p$ baryon.
\end{lstlisting}

\textbf{Reviewer Comment:}

The sentence is poorly phrased and contains a grammatical error. Use 'but' without a semicolon.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	PID is not required for the $\pi^-$ meson but is required for the $p$ baryon.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.3] [low]}

4, beginning
\begin{lstlisting}[breaklines=true]
	Events in the $\Lambda$, $\bar{\Xi}^0$, and $K^0_S$ sideband regions is scaled by a factor of 0.5
\end{lstlisting}

\textbf{Reviewer Comment:}

Sentence begins with a symbol/number ('Events'). Rephrase to avoid this.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The events in the $\Lambda$, $\bar{\Xi}^0$, and $K^0_S$ sideband regions are scaled by a factor of 0.5
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

4, paragraph describing the fit results and significance.
\begin{lstlisting}[breaklines=true]
	The statistical significance of each $\chi_{cJ}$ signal exceeds $10\sigma$, determined using the profile likelihood ratio test statistic $q_J=-2\ln(\mathcal{L}_J/\mathcal{L}_{\max})$.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid using 'determined' as a dangling participle. Rephrase for clarity.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The statistical significance of each $\chi_{cJ}$ signal exceeds $10\sigma$, as determined using the profile likelihood ratio test statistic $q_J=-2\ln(\mathcal{L}_J/\mathcal{L}_{\max})$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.6] [low]}

6, first sentence after the branching fraction list
\begin{lstlisting}[breaklines=true]
	These measurements will be helpful for understanding $\chi_{cJ}$ decay mechanisms. The ratios of the branching fractions...
\end{lstlisting}

\textbf{Reviewer Comment:}

Two independent clauses are presented as separate sentences without proper connection. Use a conjunction or combine them for better flow.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	These measurements will be helpful for understanding $\chi_{cJ}$ decay mechanisms. Furthermore, the ratios of the branching fractions...
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Numbers] [4.1] [\textcolor{blue}{medium}]}

5, paragraph 12
\begin{lstlisting}[breaklines=true]
	The systematic uncertainties due to the limited statistics of the mixed signal MC samples are 0.7\%, 0.6\%, and 0.7\% for  $\chi_{c0,1,2}\to  \Lambda K^0_S\bar \Xi^0$, and 0.500\%, 0.4\%, and 0.4\% for $\chi_{c0,1,2}\to \Lambda K^-\bar \Xi^+$,~respectively.
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent precision. '0.500\%' has three decimal places while others have one. Use consistent significant figures.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainties due to the limited statistics of the mixed signal MC samples are 0.7\%, 0.6\%, and 0.7\% for  $\chi_{c0,1,2}\to  \Lambda K^0_S\bar \Xi^0$, and 0.5\%, 0.4\%, and 0.4\% for $\chi_{c0,1,2}\to \Lambda K^-\bar \Xi^+$,~respectively.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Numbers] [4.1] [\textcolor{blue}{medium}]}

5, paragraph 13
\begin{lstlisting}[breaklines=true]
	0.07\% for $\mathcal{B}(K^{0}_{S} \to \pi^+ \pi^-)$, 0.800\% for $\mathcal{B}(\Lambda \to p \pi^-)$, 0.010\% for $\mathcal{B}(\bar{\Xi}^0 \to \Lambda\pi^0)$, 0.03\% for $\mathcal{B}(\bar{\Xi}^- \to \Lambda \pi^-)$, and 0.03\% for $\mathcal{B}(\pi^0 \to \gamma \gamma)$,
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent precision. Values have 2, 3, 3, 2, and 2 decimal places respectively. Use consistent significant figures.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	0.07\% for $\mathcal{B}(K^{0}_{S} \to \pi^+ \pi^-)$, 0.80\% for $\mathcal{B}(\Lambda \to p \pi^-)$, 0.01\% for $\mathcal{B}(\bar{\Xi}^0 \to \Lambda\pi^0)$, 0.03\% for $\mathcal{B}(\bar{\Xi}^- \to \Lambda \pi^-)$, and 0.03\% for $\mathcal{B}(\pi^0 \to \gamma \gamma)$,
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Numbers] [4.1] [\textcolor{blue}{medium}]}

6, branching fraction for $\chi_{c1}\to \lambda k^0_s\bar \xi^0$
\begin{lstlisting}[breaklines=true]
	$\mathcal{B}(\chi_{c1}\to \Lambda K^0_S\bar \Xi^0)=(7.6\pm0.8\pm0.7)\times10^{-5}$
\end{lstlisting}

\textbf{Reviewer Comment:}

Precision mismatch: central value (7.6) has one decimal, uncertainties (0.8, 0.7) also have one decimal, but the central value's precision should match the most precise uncertainty. 7.6 is acceptable, but 7.60 would align better with two-decimal uncertainties if they were given as such. Here, it's borderline but the single decimal in the central value is slightly inconsistent with the implied precision of the two-decimal uncertainties in other measurements.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	$\mathcal{B}(\chi_{c1}\to \Lambda K^0_S\bar \Xi^0)=(7.60\pm0.80\pm0.70)\times10^{-5}$ (or adjust all to consistent decimal places)
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Numbers] [4.2] [low]}

1, third paragraph, second sentence
\begin{lstlisting}[breaklines=true]
	by analyzing $(2712.4\pm14.3)\times10^6$ $\psi(3686)$ events~\cite{ref::psip-num-inc}
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing space between number and unit. Add space before 'events'.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	by analyzing $(2712.4\pm14.3)\times10^6$ $\psi(3686)$ events~\cite{ref::psip-num-inc}
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Numbers] [4.2] [low]}

2, fourth paragraph, number of decays
\begin{lstlisting}[breaklines=true]
	containing $2.7\times10^{9}$ generic $\psi(3686)$ decays
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing space between number and unit in scientific notation.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	containing $2.7\times 10^{9}$ generic $\psi(3686)$ decays
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [References] [9.7] [\textcolor{blue}{medium}]}

7, bibliography entry for ref::psip-num-inc
\begin{lstlisting}[breaklines=true]
	\href{http://arxiv.org/abs/2403.06766}{arXiv 2403 06776}
\end{lstlisting}

\textbf{Reviewer Comment:}

arXiv identifier is incorrectly formatted. It should use the standard arXiv:YYMM.NNNNN format.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	\href{https://arxiv.org/abs/2403.06766}{arXiv:2403.06766}
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [\textcolor{blue}{medium}]}

1, first paragraph, third sentence
\begin{lstlisting}[breaklines=true]
	At BESIII, except for tiny production of $e^+e^-\to \chi_{c1}$ through annihilation into two photons~\cite{ref::eetochic1}, the $\chi_{cJ}$ mesons can not be produced directly through $e^+ e^-$ annihilation processes, but can be accessed via abundant radiative decays of $\psi(3686)$ with a branching fraction of about 9\%~\cite{ref::pdg2024}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing space before opening parenthesis in citation. Use protected space before \textbackslash\{\}cite.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	At BESIII, except for tiny production of $e^+e^-\to \chi_{c1}$ through annihilation into two photons~\cite{ref::eetochic1}, the $\chi_{cJ}$ mesons can not be produced directly through $e^+ e^-$ annihilation processes, but can be accessed via abundant radiative decays of $\psi(3686)$ with a branching fraction of about 9\%~\cite{ref::pdg2024}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [\textcolor{blue}{medium}]}

1, second paragraph, fourth sentence
\begin{lstlisting}[breaklines=true]
	Besides, an enhancement in the $ p \bar p$ invariant mass spectrum has been observed in the study of $J/\psi \to \gamma p\bar p$ ~\cite{ref::ppbar1,ref::ppbar2}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space before citation. Use '\textasciitilde{}' before \textbackslash\{\}cite.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Besides, an enhancement in the $p\bar p$ invariant mass spectrum has been observed in the study of $J/\psi \to \gamma p\bar p$~\cite{ref::ppbar1,ref::ppbar2}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [\textcolor{blue}{medium}]}

1, second paragraph, second sentence
\begin{lstlisting}[breaklines=true]
	However, there is only limited information on decays of $ \chi_{cJ}$ into a $ \Lambda \bar{\Xi}$ pair and a meson, both in theory and in experiment.
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent spacing around $\chi_{cJ}$ and $\Lambda\bar{\Xi}$. Remove extra spaces inside math mode.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	However, there is only limited information on decays of $\chi_{cJ}$ into a $\Lambda\bar{\Xi}$ pair and a meson, both in theory and in experiment.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.3] [\textcolor{blue}{medium}]}

4, caption for figure labeled 'tab:lambdakxi'.
\begin{lstlisting}[breaklines=true]
	The simultaneous fits to the $M_{\Lambda K \bar \Xi}$ distributions in the $\bar \Xi$, $\Lambda$, (and $K^0_S$) signal and sideband regions for (top) $\chi_{cJ}\to \Lambda K^0_S \bar \Xi^0$ and (bottom) $\chi_{cJ}\to \Lambda K^- \bar \Xi^+$.
\end{lstlisting}

\textbf{Reviewer Comment:}

The word 'Figure' is not used at the start of this sentence (it's a caption). However, the phrase '(and $K^0_S$)' is awkwardly parenthesized. Use a clearer list structure.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Simultaneous fits to the $M_{\Lambda K \bar \Xi}$ distributions in the $\bar{\Xi}$, $\Lambda$, and $K^0_S$ signal and sideband regions for (top) $\chi_{cJ}\to \Lambda K^0_S \bar{\Xi}^0$ and (bottom) $\chi_{cJ}\to \Lambda K^- \bar{\Xi}^+$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.9] [\textcolor{blue}{medium}]}

4, equation for the product of branching fractions.
\begin{lstlisting}[breaklines=true]
	{\mathcal B}(\psi(3686)\to \gamma\chi_{cJ}) \cdot \mathcal{B}(\chi_{cJ}\to \Lambda K \bar \Xi)
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent notation for branching fraction. Uses both \{\textbackslash\{\}mathcal B\} and \textbackslash\{\}mathcal\{B\}. Choose one style and use it consistently.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	\mathcal{B}(\psi(3686)\to \gamma\chi_{cJ}) \cdot \mathcal{B}(\chi_{cJ}\to \Lambda K \bar \Xi)
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [\textcolor{blue}{medium}]}

7, acknowledgement section, first sentence
\begin{lstlisting}[breaklines=true]
	BEPCII {\hypersetup{urlcolor=black}\href{https://cstr.cn/31109.02.BEPC}{(https://cstr.cn/31109.02.BEPC)}}
\end{lstlisting}

\textbf{Reviewer Comment:}

The hyperlink is incorrectly formatted with LaTeX commands inside the text. Use a standard \textbackslash\{\}href command without extra formatting.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	BEPCII (\href{https://cstr.cn/31109.02.BEPC}{https://cstr.cn/31109.02.BEPC})
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

2, first paragraph, after 'bepcii storage ring'
\begin{lstlisting}[breaklines=true]
	BEPCII storage ring~\cite{ref::collider}
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	BEPCII storage ring~\cite{ref::collider}
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.7] [low]}

2, first paragraph, energy unit
\begin{lstlisting}[breaklines=true]
	at $\sqrt{s} = 3.773\text{GeV}$.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing space between number and unit. Units should be in roman font.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	at $\sqrt{s} = 3.773~\text{GeV}$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

2, first sentence, after 'besiii detector'
\begin{lstlisting}[breaklines=true]
	The BESIII detector~\cite{ref::detector} records symmetric $e^+e^-$ collisions
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The BESIII detector~\cite{ref::detector} records symmetric $e^+e^-$ collisions
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

2, fourth paragraph, after 'evtgen'
\begin{lstlisting}[breaklines=true]
	with {\sc evtgen}~\cite{Lange01,Lange02}
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	with \textsc{EvtGen}~\cite{Lange01,Lange02}
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

2, fourth paragraph, after 'kkmc'
\begin{lstlisting}[breaklines=true]
	in {\sc kkmc}~\cite{Jadach01}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	in \textsc{KKMC}~\cite{Jadach01}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

2, fourth paragraph, after 'lundcharm'
\begin{lstlisting}[breaklines=true]
	with {\sc lundcharm}~\cite{Lundcharm00}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	with \textsc{Lundcharm}~\cite{Lundcharm00}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

2, fourth paragraph, after 'photos package'
\begin{lstlisting}[breaklines=true]
	photos package~\cite{PHOTOS}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	\textsc{PHOTOS} package~\cite{PHOTOS}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.7] [low]}

2, second paragraph, momentum unit
\begin{lstlisting}[breaklines=true]
	at $1~{\rm GeV}/c$ is $0.5\%$.
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent unit formatting. Use math mode for the unit 'c'.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	at $1~\text{GeV}/c$ is $0.5\%$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

2, third paragraph, after 'geant4-based'
\begin{lstlisting}[breaklines=true]
	{\sc geant4}-based~\cite{Geant4} Monte Carlo
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	\textsc{Geant4}-based~\cite{Geant4} Monte Carlo
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

3, paragraph 6, sentence about $k_s^0$ signal region
\begin{lstlisting}[breaklines=true]
	The $K^{0}_{S}$ signal region is set to be $|M_{\pi^+ \pi^-}-m_{K^{0}_{S}}|<0.012~\mathrm{GeV/}$${c}^2$, where $m_{K^{0}_{S}}$ is the $K^0_S$ nominal mass~\cite{ref::pdg2024}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing a protected space (\textasciitilde{}) before the \textbackslash\{\}cite command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The $K^{0}_{S}$ signal region is set to be $|M_{\pi^+ \pi^-}-m_{K^{0}_{S}}|<0.012~\mathrm{GeV/}$${c}^2$, where $m_{K^{0}_{S}}$ is the $K^0_S$ nominal mass~\cite{ref::pdg2024}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

3, paragraph describing $\lambda$ selection
\begin{lstlisting}[breaklines=true]
	The $\Lambda$ signal region is set to be $|M_{p\pi^-}-m_{\Lambda}|<0.004~\mathrm{GeV}/c^2$, and the $\Lambda$ sideband region is set to be $0.008<|M_{p \pi^-}-m_{\Lambda}|<0.016~\mathrm{GeV}/c^2$, where $m_{\Lambda}$ is the $\Lambda$ nominal mass~\cite{ref::pdg2024}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing a protected space (\textasciitilde{}) before the \textbackslash\{\}cite command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The $\Lambda$ signal region is set to be $|M_{p\pi^-}-m_{\Lambda}|<0.004~\mathrm{GeV}/c^2$, and the $\Lambda$ sideband region is set to be $0.008<|M_{p \pi^-}-m_{\Lambda}|<0.016~\mathrm{GeV}/c^2$, where $m_{\Lambda}$ is the $\Lambda$ nominal mass~\cite{ref::pdg2024}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.7] [low]}

4, definition of the damping factor $d(e_{\gamma})$.
\begin{lstlisting}[breaklines=true]
	$D^{\mathrm{KEDR}}=\frac{E_0^2}{E_{\gamma}E_0+(E_{\gamma}-E_0)^2}$
\end{lstlisting}

\textbf{Reviewer Comment:}

The superscript 'KEDR' refers to an acronym (KEDR collaboration) and should be in roman font, not math italic. Use \textbackslash\{\}mathrm\{\}.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	$D^{\mathrm{KEDR}}=\frac{E_0^2}{E_{\gamma}E_0+(E_{\gamma}-E_0)^2}$
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

4, sentence referencing figure 2 and 3.
\begin{lstlisting}[breaklines=true]
	Figure~\ref{fig:compare_LXiKS} and \ref{fig:compare_LXiK} show the comparisons
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the second \textbackslash\{\}ref command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Figure~\ref{fig:compare_LXiKS} and~\ref{fig:compare_LXiK} show the comparisons
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

5, paragraph 2, second sentence
\begin{lstlisting}[breaklines=true]
	The uncertainty of $N_{\rm \psi(3686)}$ is 0.5\%.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing space before the percent sign. There should be a non-breaking space.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The uncertainty of $N_{\rm \psi(3686)}$ is 0.5\%.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

5, paragraph 3
\begin{lstlisting}[breaklines=true]
	They are assigned as 1.0\% for tracking or PID per track\cite{ref::tracking}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing non-breaking space before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	They are assigned as 1.0\% for tracking or PID per track~\cite{ref::tracking}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

5, paragraph 4
\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of the $K^0_S$ reconstruction is assigned to be 1.5\%\cite{ref::kso}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing non-breaking space before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of the $K^0_S$ reconstruction is assigned to be 1.5\%~\cite{ref::kso}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

5, paragraph 6, first sentence
\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of $\bar\Xi^+$ reconstruction is estimated to be $5.1\%$, including the tracking and PID of the $\bar{\Lambda}\pi^+$ pair, $\Lambda$ reconstruction, decay length cut, mass window cut, vertex fit and secondary vertex fit, by studying the control sample of $\psi(3686)\to\Xi^-\bar{\Xi}^+$ as in Ref.\cite{ref::Xi}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing non-breaking space before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of $\bar\Xi^+$ reconstruction is estimated to be $5.1\%$, including the tracking and PID of the $\bar{\Lambda}\pi^+$ pair, $\Lambda$ reconstruction, decay length cut, mass window cut, vertex fit and secondary vertex fit, by studying the control sample of $\psi(3686)\to\Xi^-\bar{\Xi}^+$ as in Ref.~\cite{ref::Xi}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

5, paragraph 7
\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of photon or $\pi^0$ reconstruction is assigned as  1.0\% per photon based on the control sample of $J/\psi\to\pi^+\pi^-\pi^0$\cite{ref::gamma-recon}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing non-breaking space before the citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of photon or $\pi^0$ reconstruction is assigned as  1.0\% per photon based on the control sample of $J/\psi\to\pi^+\pi^-\pi^0$~\cite{ref::gamma-recon}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

5, table caption
\begin{lstlisting}[breaklines=true]
	\caption{Relative systematic uncertainties (in \%) in the measurements of the branching fractions of $\chi_{cJ} \to \Lambda K^0_S \bar \Xi^0$ and $\chi_{cJ} \to \Lambda K^- \bar \Xi^+$.}
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing non-breaking space before the closing period of the caption.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	\caption{Relative systematic uncertainties (in \%) in the measurements of the branching fractions of $\chi_{cJ} \to \Lambda K^0_S \bar \Xi^0$ and $\chi_{cJ} \to \Lambda K^- \bar \Xi^+$.}
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

6, list of branching fractions
\begin{lstlisting}[breaklines=true]
	$\mathcal{B}(\chi_{c0}\to \Lambda K^0_S\bar \Xi^0)=(1.14\pm0.13\pm0.13)\times10^{-4}$, $\mathcal{B}(\chi_{c1}\to \Lambda K^0_S\bar \Xi^0)=(7.6\pm0.8\pm0.7)\times10^{-5}$, ...
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing spaces after commas separating the listed measurements. Ensure proper spacing for readability.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	$\mathcal{B}(\chi_{c0}\to \Lambda K^0_S\bar \Xi^0)=(1.14\pm0.13\pm0.13)\times10^{-4}$, $\mathcal{B}(\chi_{c1}\to \Lambda K^0_S\bar \Xi^0)=(7.6\pm0.8\pm0.7)\times10^{-5}$, ...
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.7] [low]}

7, bibliography entry for ref::lxik
\begin{lstlisting}[breaklines=true]
	Phys. Rev. D. {\bf 91}, 092006 (2015).
\end{lstlisting}

\textbf{Reviewer Comment:}

Extra period after 'D' in journal abbreviation. Should be 'Phys. Rev. D' not 'Phys. Rev. D.'

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Phys. Rev. D {\bf 91}, 092006 (2015).
\end{lstlisting}

\end{reviewer}

\end{document}
