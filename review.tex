\documentclass[12pt]{article}
\usepackage{response}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{lipsum} % to generate some filler text
\usepackage{fullpage}
\usepackage{subfigure}
\usepackage{mathrsfs}
\usepackage{xr}
\usepackage{amsthm,cite,url,graphicx,booktabs,lipsum,color,bm,caption,subcaption,soul}
\usepackage{pifont,tikz,paralist,multirow,amssymb}
\usepackage{epstopdf}
\usepackage{xifthen}
\usepackage{enumerate}
\usepackage{titlesec,upgreek}
\usepackage{lineno}
\usepackage[table]{xcolor} 
\usepackage{amsmath}
\usepackage{listings}

\definecolor{myBlue}{RGB}{0, 0, 255}
\definecolor{myRed}{RGB}{255, 0, 0}
\definecolor{myPurple}{RGB}{199, 21, 133}
\definecolor{myGreen}{RGB}{0, 128, 0}

\title{Comments}

\begin{document}
	\maketitle

\begin{reviewer}
\noindent \textbf{Category: [Abbreviations] [5.1] [low]}

\noindent \textbf{Location: [2, first mention of 'monte carlo']}

\begin{lstlisting}[breaklines=true]
	Simulated data samples produced with a {\sc geant4}-based~\cite{Geant4} Monte Carlo (MC) package
\end{lstlisting}

\textbf{Reviewer Comment:}

The abbreviation 'MC' is defined correctly at first occurrence.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Simulated data samples produced with a {\sc geant4}-based~\cite{Geant4} Monte Carlo (MC) package
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Abbreviations] [5.2] [low]}

\noindent \textbf{Location: [3, first mention of 'pid']}

\begin{lstlisting}[breaklines=true]
	Particle identification~(PID) for charged tracks combines measurements
\end{lstlisting}

\textbf{Reviewer Comment:}

Do not spell out 'Particle Identification' when defining the abbreviation 'PID'. Just use 'PID'.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Particle identification (PID) for charged tracks combines measurements
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

\noindent \textbf{Location: [1, third paragraph]}

\begin{lstlisting}[breaklines=true]
	Naive isospin symmetry indicates...
\end{lstlisting}

\textbf{Reviewer Comment:}

The term 'naive' is informal jargon. Use more precise scientific language.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Simple isospin symmetry considerations indicate...
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

\noindent \textbf{Location: [2, paragraph describing the tof system]}

\begin{lstlisting}[breaklines=true]
	The time resolution in the TOF barrel region is 68~ps, while that in the end cap region was 110~ps.
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent verb tense ('is' vs. 'was') for describing the same type of detector property.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The time resolution in the TOF barrel region is 68~ps, while that in the end cap region is 110~ps.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.3] [\textcolor{blue}{medium}]}

\noindent \textbf{Location: [4, beginning]}

\begin{lstlisting}[breaklines=true]
	Figure~\ref{fig:compare_LXiKS} and \ref{fig:compare_LXiK} show the comparisons...
\end{lstlisting}

\textbf{Reviewer Comment:}

Sentence starts with a symbol (Figure). Use the full word 'Figure' at the beginning of a sentence.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Comparisons of the two-body invariant-mass distributions for the $\chi_{cJ} \to \Lambda K^0_S\bar{\Xi}^0$ and $\chi_{cJ} \to \Lambda K^-\bar{\Xi}^+$ decays in data and mixed signal MC samples are shown in Fig.~\ref{fig:compare_LXiKS} and Fig.~\ref{fig:compare_LXiK}, respectively.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [\textcolor{blue}{medium}]}

\noindent \textbf{Location: [4, sentence describing the scaling of sideband events for the charged mode.]}

\begin{lstlisting}[breaklines=true]
	Events in the $\Lambda$ and $\bar{\Xi}^+$ sideband regions are scaled by the factors of 0.5 and 1.0, according to the relative size of signal and sideband regions.
\end{lstlisting}

\textbf{Reviewer Comment:}

The phrase 'relative size' is ambiguous. Specify if it refers to the width or yield ratio.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Events in the $\Lambda$ and $\bar{\Xi}^+$ sideband regions are scaled by factors of 0.5 and 1.0, respectively, corresponding to the ratio of background yields in the signal and sideband regions.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.6] [\textcolor{blue}{medium}]}

\noindent \textbf{Location: [6, first sentence after the number of events]}

\begin{lstlisting}[breaklines=true]
	the hadronic decays of $\chi_{cJ}\to \Lambda K^0_S\bar \Xi^0$ are observed for the first time, with  significances greater than $10\sigma$, the decays of $\chi_{cJ}\to \Lambda K^-\bar \Xi^+$ are measured with improved precision.
\end{lstlisting}

\textbf{Reviewer Comment:}

Two independent clauses are connected by a comma without a conjunction.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	the hadronic decays of $\chi_{cJ}\to \Lambda K^0_S\bar \Xi^0$ are observed for the first time, with significances greater than $10\sigma$, and the decays of $\chi_{cJ}\to \Lambda K^-\bar \Xi^+$ are measured with improved precision.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.3] [low]}

\noindent \textbf{Location: [1, first sentence of second paragraph]}

\begin{lstlisting}[breaklines=true]
	To date, there have been many studies of $\chi_{cJ}$ decays...
\end{lstlisting}

\textbf{Reviewer Comment:}

Sentence starts with a particle symbol. Rewrite to avoid beginning with a symbol.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Many studies of $\chi_{cJ}$ decays have been performed to date...
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

\noindent \textbf{Location: [2, sentence about the inclusive mc sample]}

\begin{lstlisting}[breaklines=true]
	An inclusive MC sample containing $2.7\times10^{9}$ generic $\psi(3686)$ decays is used to study background.
\end{lstlisting}

\textbf{Reviewer Comment:}

The term 'background' is singular, but the context implies multiple sources. Use the plural form.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	An inclusive MC sample containing $2.7\times10^{9}$ generic $\psi(3686)$ decays is used to study backgrounds.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

\noindent \textbf{Location: [3, description of the $k_s^0$ and $\lambda$ vertex fit requirement]}

\begin{lstlisting}[breaklines=true]
	The primary vertex fit is applied to further suppress the mis-combination, requiring the decay length divided by its uncertainty to be larger than two.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid jargon 'mis-combination'. Use 'background combinations' or 'combinatorial background'.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The primary vertex fit is applied to further suppress combinatorial background, requiring the decay length divided by its uncertainty to be larger than two.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.2] [low]}

\noindent \textbf{Location: [3, first sentence, after decay chain description]}

\begin{lstlisting}[breaklines=true]
	Signal events are required to contain at least six charged tracks and three photon candidates for $\chi_{cJ} \to \Lambda K^0_S\bar \Xi^0$,
	and at least six charged tracks and one photon candidate for $\chi_{cJ} \to \Lambda K^-\bar \Xi^+$.
\end{lstlisting}

\textbf{Reviewer Comment:}

The decay descriptor '$\chi_{cJ} \to \Lambda K^0_S\bar \Xi^0$' is used as a noun. Add a noun like 'channel' or 'decay'.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Signal events are required to contain at least six charged tracks and three photon candidates for the $\chi_{cJ} \to \Lambda K^0_S\bar \Xi^0$ channel, and at least six charged tracks and one photon candidate for the $\chi_{cJ} \to \Lambda K^-\bar \Xi^+$ channel.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

\noindent \textbf{Location: [4, paragraph describing the fit results and significance.]}

\begin{lstlisting}[breaklines=true]
	The statistical significance of each $\chi_{cJ}$ signal exceeds $10\sigma$, determined using the profile likelihood ratio test statistic $q_J=-2\ln(\mathcal{L}_J/\mathcal{L}_{\max})$.
\end{lstlisting}

\textbf{Reviewer Comment:}

The phrase 'determined using' is passive and slightly vague. Use more active and precise language.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The statistical significance of each $\chi_{cJ}$ signal exceeds $10\sigma$, as evaluated using the profile likelihood ratio test statistic $q_J=-2\ln(\mathcal{L}_J/\mathcal{L}_{\max})$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

\noindent \textbf{Location: [5, sentence: 'the systematic uncertainties of the tracking or pid for $k^{\pm}$ and $p(\bar{p})$ are estimated...']}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainties of the tracking or PID for $K^{\pm}$ and $p(\bar{p})$ are estimated with the control samples of $J/\psi\to K^{+}K^{-}\pi^0$ and $J/\psi \to p\bar p \pi^+\pi^-$.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid using particle symbols as nouns. Replace with a descriptive noun.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainties of the tracking or PID for $K^{\pm}$ and $p(\bar{p})$ tracks are estimated with the control samples of $J/\psi\to K^{+}K^{-}\pi^0$ and $J/\psi \to p\bar p \pi^+\pi^-$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Language] [1.5] [low]}

\noindent \textbf{Location: [5, sentence: 'the systematic uncertainty of the $k^0_s$ reconstruction, including tracking, $k^0_s$ mass window, vertex fit, and secondary vertex fit, is estimated...']}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of the $K^0_S$ reconstruction, including tracking, $K^0_S$ mass window, vertex fit, and secondary vertex fit, is estimated using the control sample of $J/\psi \rightarrow K^{*\pm} \bar{K}^\mp$ and $J/\psi \rightarrow \phi K^{*\pm} \bar{K}^\mp$.
\end{lstlisting}

\textbf{Reviewer Comment:}

Avoid using particle symbols as nouns. Replace with a descriptive noun.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of the $K^0_S$ reconstruction, including tracking, the $K^0_S$ mass window, vertex fit, and secondary vertex fit, is estimated using the control sample of $J/\psi \rightarrow K^{*\pm} \bar{K}^\mp$ and $J/\psi \rightarrow \phi K^{*\pm} \bar{K}^\mp$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Numbers] [4.2] [\textcolor{blue}{medium}]}

\noindent \textbf{Location: [5, sentence: 'the systematic uncertainties due to the limited statistics of the mixed signal mc samples are 0.7\textbackslash\{\}\%, 0.6\textbackslash\{\}\%, and 0.7\textbackslash\{\}\% for  $\chi_{c0,1,2}\to  \lambda k^0_s\bar \xi^0$, and 0.500\textbackslash\{\}\%, 0.4\textbackslash\{\}\%, and 0.4\textbackslash\{\}\% for $\chi_{c0,1,2}\to \lambda k^-\bar \xi^+$,\textasciitilde{}respectively.']}

\begin{lstlisting}[breaklines=true]
	and 0.500\%, 0.4\%, and 0.4\% for $\chi_{c0,1,2}\to \Lambda K^-\bar \Xi^+$,~respectively.
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent precision in the list of numbers. '0.500\%' has three decimal places while others have one.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	and 0.5\%, 0.4\%, and 0.4\% for $\chi_{c0,1,2}\to \Lambda K^-\bar \Xi^+$,~respectively.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Numbers] [4.2] [\textcolor{blue}{medium}]}

\noindent \textbf{Location: [5, sentence: 'the uncertainties from the branching fractions quoted from the pdg\textasciitilde{}\textbackslash\{\}cite\{ref::pdg2024\} are 2.3\textbackslash\{\}\%, 2.8\textbackslash\{\}\%, and 2.4\textbackslash\{\}\% for $\psi(3686)\to \gamma\chi_{c0,1,2}$, 0.07\textbackslash\{\}\% for $\mathcal{b}(k^{0}_{s} \to \pi^+ \pi^-)$, 0.800\textbackslash\{\}\% for $\mathcal{b}(\lambda \to p \pi^-)$, 0.010\textbackslash\{\}\% for $\mathcal{b}(\bar{\xi}^0 \to \lambda\pi^0)$, 0.03\textbackslash\{\}\% for $\mathcal{b}(\bar{\xi}^- \to \lambda \pi^-)$, and 0.03\textbackslash\{\}\% for $\mathcal{b}(\pi^0 \to \gamma \gamma)$,']}

\begin{lstlisting}[breaklines=true]
	0.800\% for $\mathcal{B}(\Lambda \to p \pi^-)$, 0.010\% for $\mathcal{B}(\bar{\Xi}^0 \to \Lambda\pi^0)$,
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent precision in the list of numbers. '0.800\%' and '0.010\%' have three decimal places while others have two or one.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	0.80\% for $\mathcal{B}(\Lambda \to p \pi^-)$, 0.01\% for $\mathcal{B}(\bar{\Xi}^0 \to \Lambda\pi^0)$,
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Numbers] [4.1] [low]}

\noindent \textbf{Location: [6, branching fraction for $\chi_{c1}\to \lambda k^0_s\bar \xi^0$]}

\begin{lstlisting}[breaklines=true]
	$\mathcal{B}(\chi_{c1}\to \Lambda K^0_S\bar \Xi^0)=(7.6\pm0.8\pm0.7)\times10^{-5}$
\end{lstlisting}

\textbf{Reviewer Comment:}

Precision mismatch: central value has one decimal place, uncertainties have one decimal place but the second systematic uncertainty (0.7) suggests the central value could be reported as 7.6.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	$\mathcal{B}(\chi_{c1}\to \Lambda K^0_S\bar \Xi^0)=(7.6\pm0.8\pm0.7)\times10^{-5}$ (This is acceptable as the central value 7.6 has the same precision as the uncertainties 0.8 and 0.7).
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [References] [9.7] [\textcolor{blue}{medium}]}

\noindent \textbf{Location: [7, bibliography, entry ref::psip-num-inc]}

\begin{lstlisting}[breaklines=true]
	\href{http://arxiv.org/abs/2403.06766}{arXiv 2403 06776}
\end{lstlisting}

\textbf{Reviewer Comment:}

arXiv identifier is incorrect (06776 vs 06766) and formatting is inconsistent with other entries.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	\href{https://arxiv.org/abs/2403.06766}{arXiv:2403.06766}
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.4] [\textcolor{blue}{medium}]}

\noindent \textbf{Location: [3, multiple instances defining mass windows (e.g., for $\pi^0$, $k_s^0$, $\lambda$, $\bar{\xi}$)]}

\begin{lstlisting}[breaklines=true]
	its invariant mass lies within $(0.115, 0.150)$~GeV$/c^{2}$
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent range format. Use square brackets for inclusive limits or parentheses for exclusive limits, but be consistent. For mass windows, $[0.115, 0.150]$\textasciitilde{}GeV$/c^2$ is common.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	its invariant mass lies within $[0.115, 0.150]~\mathrm{GeV}/c^{2}$
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [\textcolor{blue}{medium}]}

\noindent \textbf{Location: [7, acknowledgement section, first sentence]}

\begin{lstlisting}[breaklines=true]
	BEPCII {\hypersetup{urlcolor=black}\href{https://cstr.cn/31109.02.BEPC}{(https://cstr.cn/31109.02.BEPC)}}
\end{lstlisting}

\textbf{Reviewer Comment:}

The URL should be formatted as a proper hyperlink without displaying the raw URL in parentheses. Use \textbackslash\{\}url\{\} or \textbackslash\{\}href\{\} with descriptive text.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	BEPCII (\url{https://cstr.cn/31109.02.BEPC})
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

\noindent \textbf{Location: [1, before citation in first paragraph]}

\begin{lstlisting}[breaklines=true]
	about 9\%~\cite{ref::pdg2024}
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space before citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	about 9\%~\cite{ref::pdg2024}
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

\noindent \textbf{Location: [1, before citation in second paragraph]}

\begin{lstlisting}[breaklines=true]
	in the study of $J/\psi \to \gamma p\bar p$ ~\cite{ref::ppbar1,ref::ppbar2}
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space before citation command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	in the study of $J/\psi \to \gamma p\bar p$~\cite{ref::ppbar1,ref::ppbar2}
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.7] [low]}

\noindent \textbf{Location: [2, description of momentum resolution]}

\begin{lstlisting}[breaklines=true]
	The charged-particle momentum resolution at $1~{\rm GeV}/c$ is $0.5\%$.
\end{lstlisting}

\textbf{Reviewer Comment:}

The unit 'GeV/$c$' should be in roman font. Using \{\textbackslash\{\}rm GeV\} is acceptable, but \textbackslash\{\}text\{GeV\} is more standard.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The charged-particle momentum resolution at $1~\text{GeV}/c$ is $0.5\%$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.7] [low]}

\noindent \textbf{Location: [2, first paragraph, energy unit]}

\begin{lstlisting}[breaklines=true]
	at $\sqrt{s} = 3.773\text{GeV}$.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing space between the number and the unit. Units should be in roman font.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	at $\sqrt{s} = 3.773~\text{GeV}$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

\noindent \textbf{Location: [2, first sentence, after 'besiii detector']}

\begin{lstlisting}[breaklines=true]
	The BESIII detector~\cite{ref::detector} records symmetric $e^+e^-$ collisions
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the \textbackslash\{\}cite command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The BESIII detector~\cite{ref::detector} records symmetric $e^+e^-$ collisions
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.7] [low]}

\noindent \textbf{Location: [3, photon selection criteria, energy thresholds]}

\begin{lstlisting}[breaklines=true]
	The deposited energy of each shower must exceed 25~MeV in the barrel region ($|\cos \theta|< 0.80$, where $\theta$ denotes the
	polar angle with respect to the z-axis, the symmetry axis of the MDC) and more than 50~MeV in the end cap region ($0.86 <|\cos \theta|< 0.92$).
\end{lstlisting}

\textbf{Reviewer Comment:}

The unit 'MeV' should be in roman font, not italic. Use math mode for the energy values.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The deposited energy of each shower must exceed $25~\mathrm{MeV}$ in the barrel region ($|\cos \theta|< 0.80$, where $\theta$ denotes the
	polar angle with respect to the $z$-axis, the symmetry axis of the MDC) and more than $50~\mathrm{MeV}$ in the end cap region ($0.86 <|\cos \theta|< 0.92$).
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

\noindent \textbf{Location: [3, photon selection criteria, time requirement]}

\begin{lstlisting}[breaklines=true]
	the difference between the EMC time and the event start time is required to be within [0, 700]\,ns.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing space before the opening parenthesis. Use '\textasciitilde{}' for a protected space before units.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	the difference between the EMC time and the event start time is required to be within $[0, 700]~\mathrm{ns}$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

\noindent \textbf{Location: [4, in the caption for figure\textasciitilde{}\textbackslash\{\}ref\{fig:compare\_lxiks\}]}

\begin{lstlisting}[breaklines=true]
	Events have been required to be within the (top) $\chi_{c0}$, (middle) $\chi_{c1}$, and (down) $\chi_{c2}$, mass regions
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing space after the comma before 'mass regions'.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Events have been required to be within the (top) $\chi_{c0}$, (middle) $\chi_{c1}$, and (down) $\chi_{c2}$, mass regions
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

\noindent \textbf{Location: [4, in the sentence referencing figure\textasciitilde{}\textbackslash\{\}ref\{fig:compare\_lxiks\} and \textbackslash\{\}ref\{fig:compare\_lxik\}]}

\begin{lstlisting}[breaklines=true]
	Figure~\ref{fig:compare_LXiKS} and \ref{fig:compare_LXiK} show
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the second \textbackslash\{\}ref command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Figure~\ref{fig:compare_LXiKS} and~\ref{fig:compare_LXiK} show
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

\noindent \textbf{Location: [5, sentence: 'the systematic uncertainty of photon or $\pi^0$ reconstruction is assigned as  1.0\textbackslash\{\}\% per photon...']}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of photon or $\pi^0$ reconstruction is assigned as  1.0\% per photon based on the control sample of $J/\psi\to\pi^+\pi^-\pi^0$~\cite{ref::gamma-recon}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent spacing before the percentage value. There are two spaces before '1.0\%'.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	The systematic uncertainty of photon or $\pi^0$ reconstruction is assigned as 1.0\% per photon based on the control sample of $J/\psi\to\pi^+\pi^-\pi^0$~\cite{ref::gamma-recon}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.2] [low]}

\noindent \textbf{Location: [5, sentence: 'the systematic uncertainty of photon or $\pi^0$ reconstruction is assigned as  1.0\textbackslash\{\}\% per photon...']}

\begin{lstlisting}[breaklines=true]
	based on the control sample of $J/\psi\to\pi^+\pi^-\pi^0$~\cite{ref::gamma-recon}.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing protected space (\textasciitilde{}) before the \textbackslash\{\}cite command.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	based on the control sample of $J/\psi\to\pi^+\pi^-\pi^0$~\cite{ref::gamma-recon}.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

\noindent \textbf{Location: [6, list of branching fraction measurements]}

\begin{lstlisting}[breaklines=true]
	$\mathcal{B}(\chi_{c0}\to \Lambda K^0_S\bar \Xi^0)=(1.14\pm0.13\pm0.13)\times10^{-4}$, $\mathcal{B}(\chi_{c1}\to \Lambda K^0_S\bar \Xi^0)=(7.6\pm0.8\pm0.7)\times10^{-5}$, $\mathcal{B}(\chi_{c2}\to \Lambda K^0_S\bar \Xi^0)=(1.18\pm0.11\pm0.14)\times10^{-4}$, $\mathcal{B}(\chi_{c0}\to \Lambda K^-\bar \Xi^+)=(1.95\pm0.08\pm0.17)\times10^{-4}$, $\mathcal{B}(\chi_{c1}\to \Lambda K^-\bar \Xi^+)=(1.41\pm0.05\pm0.11)\times10^{-4}$, and $\mathcal{B}(\chi_{c2}\to \Lambda K^-\bar \Xi^+)=(2.01\pm0.07\pm0.16)\times10^{-4}$.
\end{lstlisting}

\textbf{Reviewer Comment:}

Missing spaces after commas separating the list items.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	$\mathcal{B}(\chi_{c0}\to \Lambda K^0_S\bar \Xi^0)=(1.14\pm0.13\pm0.13)\times10^{-4}$, $\mathcal{B}(\chi_{c1}\to \Lambda K^0_S\bar \Xi^0)=(7.6\pm0.8\pm0.7)\times10^{-5}$, $\mathcal{B}(\chi_{c2}\to \Lambda K^0_S\bar \Xi^0)=(1.18\pm0.11\pm0.14)\times10^{-4}$, $\mathcal{B}(\chi_{c0}\to \Lambda K^-\bar \Xi^+)=(1.95\pm0.08\pm0.17)\times10^{-4}$, $\mathcal{B}(\chi_{c1}\to \Lambda K^-\bar \Xi^+)=(1.41\pm0.05\pm0.11)\times10^{-4}$, and $\mathcal{B}(\chi_{c2}\to \Lambda K^-\bar \Xi^+)=(2.01\pm0.07\pm0.16)\times10^{-4}$.
\end{lstlisting}

\end{reviewer}

\begin{reviewer}
\noindent \textbf{Category: [Typography] [3.0] [low]}

\noindent \textbf{Location: [7, bibliography, multiple entries]}

\begin{lstlisting}[breaklines=true]
	Phys. Rev. D {\bf 110}, 030001 (2024).}
\end{lstlisting}

\textbf{Reviewer Comment:}

Inconsistent spacing and punctuation in bibliography entries. Some have periods after the year, others do not.

\textbf{Suggested Revision:}

\begin{lstlisting}[breaklines=true]
	Ensure consistent formatting: Phys. Rev. D {\bf 110}, 030001 (2024).
\end{lstlisting}

\end{reviewer}

\end{document}
